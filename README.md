# koolkurkle

iCloud mail retrieve scripts.

Headers-only IMAP (8pm / imap_newmail / imap_tombstone) defaults to
Apple `/usr/bin/curl`. BODY.PEEK body fetch still uses Homebrew curl
≥8.17. See [README.imap-curl.md](README.imap-curl.md).
